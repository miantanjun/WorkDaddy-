# 本机数据与构建产物，不入库
_backup/
scripts/runtime/
release/windows/
release/.cache/

# Inno 卸载器状态（含本机安装路径，机器专属，不能跨机同步）
unins000.*
卸载 WorkDaddy.*
卸载 WorkBuddy 助手.*

# 日志
*.log
daemon.log

# Node
node_modules/
npm-debug.log*

# macOS
.DS_Store

# 构建产物与安装包
WorkDaddy.app
/release/**/*.dmg
/release/**/*.exe
/release/**/*.zip
/release/**/*.pkg
/release/**/*.tar.gz
/release/**/*.deb

# 内部调试模块（元素检查/DevTools 实现，不公开入库；他人环境点按钮报错属预期）
scripts/picker-internal.js

# 工作区元数据（WorkBuddy 项目数据，不入库）
.workbuddy/
.workbuddy-ai/

# 本地维护文档与排查记录
/.private-docs/

# .wd-analysis 只入库脚本：真机截图与真实扫描结果是本机证据，可随时重生成
# （脚本：cdp-eval.py / probe-*.js / real-space-scan.js / rescan-and-report.py）
.wd-analysis/*.png
.wd-analysis/real-space-scan.json
# probe-*/test-*/verify-* 脚本的文本报告同为「本机证据」，不入库
.wd-analysis/*.out.txt
.wd-analysis/*.report.txt
